# -*- mode: snippet -*-
#name: elisp file header
# --
;;; `(file-name-nondirectory (buffer-file-name))` --- ${1:Summary}
;;; Commentary:
;;
;;  ${2:Musings}
;;
;;; Code:

$0

;;; `(file-name-nondirectory (buffer-file-name))` ends here
